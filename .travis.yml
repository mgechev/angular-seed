language: node_js
sudo: false
node_js:
  - 'stable'

addons:
  sauce_connect: true

cache:
  directories:
    - node_modules

env:
  global:
  - SAUCE_USERNAME=Shyam-Chen  # to add your USERNAME
  - SAUCE_ACCESS_KEY=c3608032-6eff-452e-9027-602ca80a8778  # to add your ACCESS_KEY

branches:
  only:
  - master

before_script:
  - sleep 1

script:
  - gulp build.prod --color && gulp build.js.e2e --color && gulp run.e2e --color
