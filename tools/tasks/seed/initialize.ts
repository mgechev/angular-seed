/**
 * This task is run at the start of every build (see /tools/config/seed.tasks.json)
 * The task is currently inert (does nothing).
 * You can replace it with your own initialization task by putting a file at /tools/tasks/project/initialize.ts
 */
export = (done: any) => {
    done();
};
