/**
 * This task is run at the start of every build (see /tools/config/seed.tasks.json)
 * The task is currently inert (does nothing).
 * Replace this inert task with your own if you wish to run the same startup script before every build
 */
export = (done: any) => {
  done();
};
